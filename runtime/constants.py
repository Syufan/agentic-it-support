"""State-machine constants, not deployment config."""

MAX_RESOLUTION_ATTEMPTS = 2

# Two successful distinct tool sources.
CONFIDENCE_HIGH = 0.7

# One successful distinct tool source.
CONFIDENCE_RESOLVE_MIN = 0.35